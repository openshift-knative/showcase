class Info {
  constructor({ project, config }) {

    /**
    * @type {Project}
    */
    this.project = project

    /**
    * @type {Config}
    */
    this.config = config
  }
}

module.exports = Info
